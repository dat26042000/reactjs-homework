const compare = (a) => {
    if(a > 100)
    {
        console.log ('Số '+a+' > 100')
    } 
    else if(a < 100)
    {
        console.log ('Số '+a+' < 100')
    }
    else 
    console.log ('Số vừa nhập = 100')
}